# 快速开始

本文档帮助你在 5 分钟内上手 MiniMind。

## 📋 前置要求

- Python 3.9+
- CUDA 11.8+（GPU 训练）
- 8GB+ 显存（推荐 16GB+）

## 🔧 安装

### 1. 克隆项目

```bash
git clone https://github.com/jingyaogong/minimind.git
cd minimind
```

### 2. 安装依赖

```bash
pip install -r requirements.txt
```

### 3. 下载预训练权重

从 [ModelScope](https://modelscope.cn/models/gongjy/MiniMind-V1) 或 [HuggingFace](https://huggingface.co/jingyaogong/MiniMind-V1) 下载权重文件，放入 `out/` 目录。

## 🚀 快速体验

### 对话推理

```bash
# 使用默认配置进行对话
python scripts/eval_llm.py

# 选择模式：
# [0] 自动测试 - 使用预设问题测试
# [1] 手动输入 - 自由对话
```

### 查看模型信息

```python
from model.model_minimind import MiniMindConfig, MiniMindForCausalLM

# 创建配置
config = MiniMindConfig(hidden_size=512, num_hidden_layers=8)

# 创建模型
model = MiniMindForCausalLM(config)

# 查看参数量
print(f"参数量: {sum(p.numel() for p in model.parameters()) / 1e6:.2f}M")
```

## 📊 模型规格

| 模型 | hidden_size | num_layers | 参数量 | 显存需求 |
|------|-------------|------------|--------|----------|
| MiniMind-Small | 512 | 8 | 26M | ~2GB |
| MiniMind-Base | 768 | 16 | 104M | ~4GB |
| MiniMind-MoE | 640 | 8 | 145M | ~6GB |

## 🎯 下一步

- 📖 阅读 [训练指南](./训练指南.md) 了解如何训练模型
- 🔧 查看 [命令行参数大全](./命令行参数大全.md) 了解所有配置选项
- 💡 参考 [操作示例](./操作示例.md) 获取更多使用场景

## ❓ 常见问题

### Q: 显存不足怎么办？

A: 尝试以下方法：
1. 减小 `batch_size`
2. 使用梯度累积 `--accumulation_steps`
3. 使用更小的模型 `--hidden_size 512`

### Q: 如何使用 CPU 运行？

A: 添加 `--device cpu` 参数：
```bash
python scripts/eval_llm.py --device cpu
```

### Q: 如何加载自己训练的权重？

A: 指定权重路径：
```bash
python scripts/eval_llm.py --weight my_model --save_dir ./my_weights